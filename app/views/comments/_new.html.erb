<div class="comment-form">
  <%= form_for [ @tutorial, Comment.new ], remote: true, class: 'form-group' do |f| %>
    <%= f.text_area :body, class: 'form-control' %><br>
    <%= f.submit t('comments.new.submit'), class: 'btn btn-primary' %>
  <% end %>
</div>